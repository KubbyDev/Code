#ifndef TESTS_PENDU_H
#define TESTS_PENDU_H

char* generateWord();
int rnd(int min, int max);
char readChar();
int replaceChars(char* info, const char* word, char guess, unsigned int length);

#endif //TESTS_PENDU_H